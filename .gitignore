.vercel
.env*

# Added with the login backend — bcryptjs and its transitive deps. Never commit
# this: it is large, platform-specific, and rebuilt from package-lock.json.
node_modules/

# Agent skills installed by the Vercel/Neon integration — regenerable, and the whole
# folder is deployed, so tracking these would publish them at the site root.
.claude/skills/
.agents/
skills-lock.json

# Vite build output — regenerated by `npm run build`, and rebuilt by Vercel on deploy.
dist/

# TypeScript incremental build info.
*.tsbuildinfo
.vite/
